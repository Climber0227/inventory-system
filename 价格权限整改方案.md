# 价格权限整改方案

> 目标：员工(role_2)不能看到采购价/costPrice/unitPrice，管理员(role_1)不受影响。
> 请在每条的 `→` 后面写下你的修改决定。

---

## 一、后端 API — 商品模块

### 1. `GET /api/v1/product/page` — 商品分页列表
**当前**: 返回的每条 Product 含 `purchasePrice` 字段
**选项**: A=员工看不到采购价(置null) / B=维持现状 / C=员工不允许访问此接口
→ a

### 2. `GET /api/v1/product/list` — 全部启用商品
**当前**: 返回全部 Product 含 `purchasePrice`
**选项**: A=员工看不到采购价 / B=维持现状 / C=员工不允许访问
→ a

### 3. `GET /api/v1/product/{id}` — 单个商品详情
**当前**: 返回 Product 含 `purchasePrice`
**选项**: A=员工看不到采购价 / B=维持现状 / C=员工不允许访问
→ A

### 4. `GET /api/v1/product/export` — 导出商品列表
**当前**: 导出 Excel 含"采购价"列
**选项**: A=员工导出时去掉采购价列 / B=员工不允许导出 / C=维持现状
→ B

### 5. `GET /api/v1/product/export-for-import` — 导出为导入格式
**当前**: 导出 Excel 含"采购价"列
**选项**: A=员工导出时去掉采购价列 / B=员工不允许导出 / C=维持现状
→B

### 6. `GET /api/v1/product/page` 的查询参数 minPrice/maxPrice
**当前**: 前端可传 `minPrice`/`maxPrice` 按采购价范围筛选商品
**选项**: A=员工不能使用采购价筛选(忽略参数) / B=维持现状
→ A

---

## 二、后端 API — 采购订单模块

### 7. `GET /api/v1/purchase-order/page` — 采购订单列表
**当前**: 返回的订单 items 中每条含 `unitPrice`
**选项**: A=员工看不到 unitPrice / B=维持现状 / C=员工不允许看采购订单列表
→ C

### 8. `GET /api/v1/purchase-order/{id}` — 采购订单详情
**当前**: 返回订单 items 中每条含 `unitPrice`
**选项**: A=员工看不到 unitPrice / B=维持现状 / C=员工不允许看采购订单详情
→ C

### 9. `POST /api/v1/purchase-order` — 创建采购单
**当前**: 无角色限制，员工可创建，接口接收 items 中的 `unitPrice`
**选项**: A=仅管理员可创建 / B=员工可创建但单价由系统自动取采购价(不从入参取) / C=维持现状
→ A

### 10. `PUT /api/v1/purchase-order/{id}/draft` — 更新草稿采购单
**当前**: 无角色限制，员工可修改草稿
**选项**: A=仅管理员可修改 / B=维持现状
→ A

---

## 三、后端 API — 库存模块

### 11. `GET /api/v1/inventory/page` — 库存分页列表
**当前**: 每行 Inventory 含 `costPrice`(加权平均成本价)
**选项**: A=员工看不到 costPrice / B=维持现状
→ A

### 12. `GET /api/v1/inventory/export` — 导出库存
**当前**: 导出 Excel 含成本价列
**选项**: A=员工导出时去掉成本价列 / B=员工不允许导出 / C=维持现状
→ B

---

## 四、后端 API — 调拨

### 13. `GET /api/v1/transfer/{id}` — 调拨单详情
**当前**: items 中的 `unitPrice` 为源仓库成本价
**选项**: A=员工看不到 unitPrice / B=维持现状
→ A

---

## 五、后台管理前端 (Vue3)

### 14. ProductList.vue — 表格"采购价"列
**当前**: `<el-table-column prop="purchasePrice" label="采购价">`
**选项**: A=员工看不到此列(v-if=isAdmin) / B=维持现状
→ A

### 15. ProductList.vue — "最低采购价""最高采购价"筛选框
**当前**: 两个 el-input 按采购价范围筛选
**选项**: A=员工看不到筛选框 / B=维持现状
→ A

### 16. ProductList.vue — 创建/编辑表单"采购参考价"
**当前**: `<el-input-number v-model="form.purchasePrice">`
**选项**: A=员工看不到此字段 / B=员工只能看不能改 / C=维持现状
→ A

### 17. InventoryList.vue — 成本价列 + 成本金额列
**当前**: 表格显示 `costPrice` 和 `costPrice × quantity` 两列
**选项**: A=员工看不到这两列 / B=维持现状
→ A

### 18. PurchaseForm.vue — 采购单创建页
**当前**: 提示"已自动填充采购价"，表格有单价列可编辑
**选项**: A=仅管理员可访问此页面 / B=员工可访问但看不到单价 / C=维持现状
→ A

### 19. PurchaseDetail.vue — 采购单详情"单价"列
**当前**: 表格显示 `unitPrice`
**选项**: A=员工看不到单价列 / B=员工不能访问此页面 / C=维持现状
→ A

### 20. SalesDetail.vue — 销售单详情"售价"列
**当前**: 表格显示 `unitPrice`(这是售价，不是采购价)
**选项**: A=员工看不到售价列 / B=维持现状
→ B

### 21. SalesForm.vue — 销售单创建页"已自动填充销售价"
**当前**: 提示"已自动填充销售价"，表格有单价列
**选项**: A=维持现状(售价敏感度低) / B=员工看不到售价列
→ A

---

## 六、小程序前端 (uni-app)

### 22. product/list.vue — 商品卡片 + 采购价排序
**当前**: 卡片显示 `采购 ¥{{ purchasePrice }}`，有采购价排序按钮
**选项**: A=员工看不到采购价和排序 / B=维持现状
→ A

### 23. product/detail.vue — 商品详情 + 编辑表单
**当前**: 详情展示"采购价"行，编辑表单有采购价输入框
**选项**: A=员工看不到采购价 / B=员工只能看不能改 / C=员工不能编辑商品
→ C

### 24. purchase/create.vue — 采购单创建页
**当前**: 选商品后自动填充 `purchasePrice`，显示"采购价 ¥..."
**选项**: A=仅管理员可访问 / B=员工可创建但看不到采购价引用 / C=维持现状
→ A

### 25. purchase/detail.vue — 采购单详情
**当前**: 每行显示 `unitPrice`
**选项**: A=员工看不到单价 / B=员工不能访问 / C=维持现状
→ B

### 26. inventory/query.vue — 库存查询
**当前**: 显示"均价" `costPrice` + 金额计算
**选项**: A=员工看不到均价和金额 / B=维持现状
→ A

### 27. sales/create.vue — 销售单创建
**当前**: 自动填充 `salePrice`(售价，非采购价)
**选项**: A=维持现状 / B=员工看不到售价
→ A

### 28. sales/detail.vue — 销售单详情
**当前**: 显示 `unitPrice`(售价)
**选项**: A=维持现状 / B=员工看不到售价
→ A

---

## 七、权限缺口

### 29. `POST /api/v1/purchase-order` — 创建采购单（无角色限制）
**当前**: 员工可创建采购单
**选项**: A=加 @SaCheckRole("role_1") 仅管理员 / B=维持现状
→ A

### 30. `POST /api/v1/sales-order` — 创建销售单（无角色限制）
**当前**: 员工可创建销售单
**选项**: A=加限制仅管理员 / B=维持现状(员工应该能创建销售单)
→ B

### 31. `POST /api/v1/transfer` — 创建调拨单（无角色限制）
**当前**: 员工可创建调拨单
**选项**: A=加限制仅管理员 / B=维持现状
→ B

### 32. `POST /api/v1/stocktake` — 创建盘点单（无角色限制）
**当前**: 员工可创建盘点单
**选项**: A=加限制仅管理员 / B=维持现状
→ B
